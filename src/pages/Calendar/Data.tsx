import styled from "@emotion/styled";

export default function Data({ id }: { id: string }) {
  return <Container></Container>;
}

const Container = styled.div`
  width: 5rem;
  height: 40rem;
  background-color: blue;
`;
